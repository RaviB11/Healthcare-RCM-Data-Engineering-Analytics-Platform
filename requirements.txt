# Core processing -----------------------------------------------------------
pyspark==3.5.1          # matches AWS Glue 5.0; 4.x also works locally
pyyaml>=6.0.1
faker>=24.0.0

# Local warehouse / SQL execution ------------------------------------------
duckdb>=0.10.0          # runs the analytics SQL without a Redshift cluster
pandas>=2.0.0
pyarrow>=15.0.0

# AWS ----------------------------------------------------------------------
boto3>=1.34.0
awswrangler>=3.7.0

# Testing and quality ------------------------------------------------------
pytest>=8.0.0
pytest-cov>=4.1.0
ruff>=0.3.0
black>=24.0.0

# Airflow providers are intentionally NOT pinned here. They belong in the
# MWAA requirements file, not in the local development environment, where
# installing Airflow would drag in a conflicting dependency tree.
